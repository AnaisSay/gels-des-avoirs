name: ⏱️ Mise à jour fichier gels.json

on:
  schedule:
    - cron: "0 4 * * *"  # Tous les jours à 4h du matin UTC
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Télécharger le fichier JSON depuis la DGT
        run: |
          curl -sSf https://gels-avoirs.dgtresor.gouv.fr/api/v1/publication/derniere-publication-fichier-json \
            -o data/latest.json

      - name: Commit et push si modifié
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add data/latest.json
          if ! git diff --cached --quiet; then
            git commit -m "🔄 Update gels.json [auto]"
            git push
          else
            echo "Aucun changement détecté"
          fi
